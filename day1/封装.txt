面向对象设计过程：就是抽象的过程
抽象的原则：只封装和项目有关的属性方法
为什么说类图是面向对象设计的好工具：发现类，发现属性，发现方法

构造方法：（可以重载，不能重写）
1.没有返回值类型 void都没有
2.方法名与类名一致
作用：1.创建对象2.给属性赋值
如果手动添加构造默认的将消失，建议：一定保证无参构造方法的存在
方法重载：
在同一个类中，方法名相同，参数项不相同（个数，顺序，类型），与访问权限修饰符无关
静态属性方法（属于类不属于对象，静态成员内存中只有一份）：
1.可以通过类直接调用
2.也可以通过对象调用
3.静态方法不能直接调非静态成员

final常量（单词全部大写）